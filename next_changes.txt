Next Changes: Unordered list of next enhancements and bug fixes to be implemented

Second step of two-step transformation:
1. Get everything working fine: 
    1.1 Update the configuration widget for vector selection.
        DONE 1.1.1 If more than two vectors, the user can select which of the others is the third.
        1.1.2 If there are only two vectors, A defaults to 1. No selection of third vector.
    1.2 Test that transformations all work. Should be a delay with no spinner for full multivector 2-step transformation
    1.3 Add a spinner
2. A slider for A for the entire group of vectors
    Range of A is 0 to 10
3. Do some linting fixes

4. Exceptions log file. ExceptHook.pdf



LATER
*The stylesheet strings need refactored into config.py for maintainability
*Update the Help->User manual to explain the transformations and the m2 option.
*Refactor: the extra_circles needs to be stored along with selected vector names and transformation type, instead of separately stored in the controller.

